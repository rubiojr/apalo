# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/apalotk.rb'

Hoe.new('apalotk', Apalotk::VERSION) do |p|
  # p.rubyforge_name = 'apalotkx' # if different than lowercase project name
  # p.developer('FIX', 'FIX@example.com')
end

# vim: syntax=Ruby
